<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 
    see {androidSource}/frameworks/base/libs/androidfw/ResourceTypes.cpp 
    match :
      smallestScreenWidthDp
      screenWidthDp
      screenHeightDp
      screenWidth
      screenHeight
      
      if ($attribute > setting.$attribute) return false;
      discard all config which large then devices's config.
      向下就近原则
    isBetterThan :
      smallestScreenWidthDp
      screenSizeDp && screenSizeDp
      density (有点复杂)
      screenWidth && screenHeight
      
      the closest config wins.
    isMoreSpecifi
    see {androidSource}/frameworks/base/tools/aapt/AaptAssets.cpp
     -->
    <string name="place_holder">place holder</string>
</resources>
